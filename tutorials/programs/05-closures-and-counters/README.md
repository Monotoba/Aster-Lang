# Program 05: Closures and Counters

Features used: lambdas, closures (capturing `mut`), higher-order functions.

Run:

```bash
uv run aster run tutorials/programs/05-closures-and-counters/main.aster
```
