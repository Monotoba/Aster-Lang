# Program 03: Shapes and `match`

Features used: records, lists, pattern matching, or-patterns, destructuring in patterns.

Run:

```bash
uv run aster run tutorials/programs/03-shapes-and-match/main.aster
```
